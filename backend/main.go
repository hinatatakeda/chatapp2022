package main

import (
	"takehina/router"
	// "net/http"
	// "log"
)

func main() {
	router.Router()

	// http.HandleFunc("/room", router.LoginHandler)
	// if err := http.ListenAndServe(":8080", nil); err != nil {
	// 	log.Fatal(err)
	// }


}
